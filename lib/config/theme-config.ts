import type { Theme } from '@/types'

/**
 * Theme configuration
 * Change the default theme here: 'light' | 'dark' | 'system'
 */
export const defaultTheme: Theme = 'dark'

/**
 * Theme storage key for localStorage
 */
export const THEME_STORAGE_KEY = 'portfolio-theme'
